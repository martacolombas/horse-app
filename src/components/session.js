import React from 'react';
import cx from 'classnames';
import './session.css';

function Session({ className }) {
  return <div className={cx('Session', className)}></div>;
}

export default Session;
